{
	"configurations": [
		{
			"type": "byond",
			"request": "attach",
			"name": "Attach to Debugger",
			"port": 2448
		},
	{
		"type": "byond",
		"request": "launch",
		"name": "Launch DreamSeeker",
		"dmb": "${workspaceFolder}/${command:CurrentDMB}",
		"env": {
			"AUXTOOLS_DEBUG_DLL": "${workspaceFolder}/debug_server.dll"
		}
	}
	]
}
